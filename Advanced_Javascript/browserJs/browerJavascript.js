/*
you can run any code you can in the ide as well as the console in chrome 

command line api 
was made originally made by firebug, and allowed you to play with html in the broweser to be able to play with it there

you can type pure js in the api console and returns something that looks like jquery 
function fullName(first, last) {
  return `${first} ${last}`;
}

fullName('Kristine', 'Hudgens');


newFullName = (first, last) => {
  return `${first} ${last}`;
}

newFullName('Kristine', 'Hudgens');

// Source page: http://www.dailysmarty.com/

// Console with Command Line API / Firebug
$('.image-item')
$('.image-item').first // returns function
$('.image-item').first()
$('.image-item').first().hide()

// Compared with
document.getElementsByClassName('post-item-wrapper')[0].style.display = 'none';

how to process a file in the browser 


fullName - (first, last) => {
    return `${first} ${last}
}
alert(fullName('Jacob', 'Mason')) if you do this the brower will render your code, and if you want the brower to run you code you have 
to tell it to 
create a new file index.html
do html5 boilerplate, and put a h1 tag in it or whatever you want 
and put a script tag in it, and put your js in the script tag 

user snippits in vs code are structured in json  like this 
{
    "Boilerplate": {
        "prefix": "html5",
        "body": [
            "<!DOCTYPE html>",
            "<html lang='en'>",
            "<head>",
            "  <meta charset='UTF-8'>",
            "  <title></title>",
            "</head>",
            "<body>",
            "  $0",
            "</body>",
            "</html>"
        ],
*/
console.log('Hi There');

